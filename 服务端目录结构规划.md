# 服务端目录结构规划

## 📋 项目结构分析

### 当前项目结构
```
cypridina-client/
├── src/                    # 前端源代码 (SvelteKit)
├── src-tauri/              # Tauri 配置
├── build/                  # 构建输出
├── docs/                   # 文档
├── scripts/                # 脚本
├── package.json            # 前端依赖
└── ...
```

### 问题分析
- ✅ 前端项目已经很完整
- ✅ 有独立的 Tauri 配置 (src-tauri)
- ❌ 没有后端代码
- ❌ 前后端混在一起会导致依赖冲突

---

## 🎯 推荐方案：创建独立的 server 目录

### 为什么选择独立目录？

| 方案 | 优点 | 缺点 |
|------|------|------|
| **独立 server 目录** | ✅ 清晰分离、依赖独立、易于部署 | ❌ 需要管理两个项目 |
| 混在一起 | ✅ 一个 package.json | ❌ 依赖冲突、部署复杂 |
| 单独仓库 | ✅ 完全独立 | ❌ 开发不便、同步困难 |

**结论**: 选择独立 server 目录，在同一个仓库中管理

---

## 📁 推荐的项目结构

```
cypridina/                          # 项目根目录
├── client/                         # 前端项目 (当前的 src 等)
│   ├── src/
│   ├── src-tauri/
│   ├── package.json
│   └── ...
│
├── server/                         # 后端项目 (新建)
│   ├── src/
│   │   ├── config/                 # 配置文件
│   │   │   ├── database.ts         # 数据库配置
│   │   │   ├── env.ts             # 环境变量
│   │   │   └── constants.ts       # 常量
│   │   │
│   │   ├── controllers/            # 控制层
│   │   │   ├── auth.controller.ts
│   │   │   ├── sales.controller.ts
│   │   │   ├── customers.controller.ts
│   │   │   ├── products.controller.ts
│   │   │   └── ...
│   │   │
│   │   ├── services/               # 业务层
│   │   │   ├── auth.service.ts
│   │   │   ├── sales.service.ts
│   │   │   ├── customers.service.ts
│   │   │   ├── products.service.ts
│   │   │   └── ...
│   │   │
│   │   ├── models/                 # 数据模型
│   │   │   └── prisma.ts
│   │   │
│   │   ├── middleware/             # 中间件
│   │   │   ├── auth.middleware.ts
│   │   │   ├── error.middleware.ts
│   │   │   └── logger.middleware.ts
│   │   │
│   │   ├── routes/                 # 路由
│   │   │   ├── auth.routes.ts
│   │   │   ├── sales.routes.ts
│   │   │   ├── customers.routes.ts
│   │   │   ├── products.routes.ts
│   │   │   └── index.ts
│   │   │
│   │   ├── utils/                  # 工具函数
│   │   │   ├── response.ts         # 响应格式
│   │   │   ├── validators.ts       # 验证函数
│   │   │   ├── helpers.ts          # 辅助函数
│   │   │   └── errors.ts           # 错误处理
│   │   │
│   │   ├── types/                  # TypeScript 类型
│   │   │   └── index.ts
│   │   │
│   │   └── app.ts                  # 应用入口
│   │
│   ├── prisma/
│   │   ├── schema.prisma           # 数据库 schema
│   │   └── migrations/             # 数据库迁移
│   │
│   ├── tests/                      # 测试文件
│   │   ├── unit/
│   │   ├── integration/
│   │   └── e2e/
│   │
│   ├── .env                        # 环境变量
│   ├── .env.example                # 环境变量示例
│   ├── .gitignore
│   ├── package.json                # 后端依赖
│   ├── tsconfig.json               # TypeScript 配置
│   ├── docker-compose.yml          # Docker 配置
│   ├── Dockerfile                  # Docker 镜像
│   └── README.md
│
├── docs/                           # 文档
│   ├── 服务端架构设计文档.md
│   ├── API接口设计文档.md
│   ├── 数据库设计文档.md
│   └── ...
│
└── README.md                       # 项目总说明
```

---

## 🔄 迁移计划

### 第一步：重新组织项目结构
```bash
# 1. 创建 client 目录，移动前端代码
mkdir client
mv src client/
mv src-tauri client/
mv package.json client/
mv tsconfig.json client/
mv vite.config.js client/
mv svelte.config.js client/
mv tailwind.config.js client/
mv postcss.config.js client/
mv pnpm-lock.yaml client/
# ... 其他前端相关文件

# 2. 创建 server 目录
mkdir server
cd server
```

### 第二步：初始化后端项目
```bash
cd server
npm init -y
npm install express cors helmet dotenv
npm install pg prisma @prisma/client
npm install jsonwebtoken bcryptjs
npm install joi winston
npm install -D typescript ts-node @types/node @types/express
npx tsc --init
```

### 第三步：创建目录结构
```bash
mkdir -p src/{config,controllers,services,models,middleware,routes,utils,types}
mkdir -p prisma/migrations
mkdir -p tests/{unit,integration,e2e}
```

---

## 📝 文件清单

### 前端文件 (需要移动到 client/)
- src/
- src-tauri/
- package.json
- pnpm-lock.yaml
- tsconfig.json
- vite.config.js
- svelte.config.js
- tailwind.config.js
- postcss.config.js
- components.json
- build/
- static/
- scripts/
- *.sh 脚本文件

### 后端文件 (需要创建在 server/)
- src/
- prisma/
- tests/
- package.json
- tsconfig.json
- .env
- .env.example
- docker-compose.yml
- Dockerfile
- README.md

### 共享文件 (保留在根目录)
- docs/ (文档)
- README.md (项目总说明)
- .gitignore
- 各种设计文档 (.md 文件)

---

## ✅ 决策总结

### 最终方案
✅ **创建独立的 server 目录，与 client 目录并行**

### 优势
1. ✅ 清晰的项目结构
2. ✅ 前后端依赖完全独立
3. ✅ 易于部署和扩展
4. ✅ 便于团队协作
5. ✅ 符合微服务架构思想

### 实施步骤
1. 创建 client 目录，移动前端代码
2. 创建 server 目录，初始化后端项目
3. 更新根目录 README.md
4. 更新 .gitignore
5. 开始后端开发

---

## 🚀 下一步

1. ✅ 确认目录结构规划
2. ⏳ 执行迁移计划
3. ⏳ 初始化后端项目
4. ⏳ 开始开发


